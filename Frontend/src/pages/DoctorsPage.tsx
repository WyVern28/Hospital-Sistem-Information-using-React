import { useMemo, useState } from 'react'
import { doctors } from '../data/doctors'
import type { Page } from '../types'

export default function DoctorsPage({ onNavigate }: { onNavigate: (page: Page) => void }) { const [query, setQuery] = useState(''); const filtered = useMemo(() => doctors.filter(d => `${d.name} ${d.specialty}`.toLowerCase().includes(query.toLowerCase())), [query]); return <main><section className="page-hero compact"><span className="eyebrow">TENAGA MEDIS PROFESIONAL</span><h1>Dokter & Jadwal Praktik</h1><p>Pilih dokter spesialis dan waktu konsultasi yang sesuai untuk Anda.</p><div className="search"><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Cari nama dokter atau spesialis..." /><span>⌕</span></div></section><section className="section"><div className="doctor-grid">{filtered.map(d => <article className="doctor-card" key={d.name}><div className="doctor-avatar">{d.initial}</div><div><span className="availability">Tersedia</span><h3>{d.name}</h3><p>{d.specialty}</p><dl><div><dt>Hari praktik</dt><dd>{d.day}</dd></div><div><dt>Jam praktik</dt><dd>{d.time} WIB</dd></div></dl><button className="button primary small" onClick={() => onNavigate('registration')}>Buat Janji</button></div></article>)}</div></section></main> }

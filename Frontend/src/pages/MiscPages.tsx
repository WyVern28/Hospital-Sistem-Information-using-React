import { type FormEvent, useState } from 'react'
import Field from '../components/ui/Field'

export function ContactPage() { return <main><section className="page-hero compact"><span className="eyebrow">KONTAK & LOKASI</span><h1>Kami siap membantu Anda</h1><p>Hubungi Anahita Hospital untuk informasi layanan, jadwal, atau keadaan darurat.</p></section><section className="section contact-grid"><article className="contact-info"><h2>Informasi kontak</h2><p><b>Alamat</b><br />123 Healthcare Way, Medical District, Jakarta</p><p><b>Telepon</b><br />(555) 0123-4567</p><p><b>Email</b><br />care@anahitahospital.id</p><p><b>Jam operasional</b><br />Senin–Minggu, 24 jam</p></article><form className="form-card" onSubmit={e => e.preventDefault()}><h2>Kirim pesan</h2><Field label="Nama"><input placeholder="Nama Anda" /></Field><Field label="Email"><input type="email" placeholder="nama@email.com" /></Field><Field label="Pesan"><textarea placeholder="Apa yang bisa kami bantu?" /></Field><button className="button primary">Kirim Pesan</button></form></section></main> }
export function LoginPage({ onSuccess, onSignup }: { onSuccess: () => void; onSignup: () => void }) { const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const submit = (e: FormEvent) => { e.preventDefault(); if (!email || !password) return setError('Silakan isi email dan kata sandi.'); onSuccess() }; return <main className="auth-page"><form className="auth-card" onSubmit={submit}><span className="brand-mark large">+</span><h1>Masuk ke akun pasien</h1><p>Kelola pendaftaran dan riwayat kunjungan Anda.</p><Field label="Email atau nomor telepon"><input value={email} onChange={e => setEmail(e.target.value)} /></Field><Field label="Kata sandi"><input type="password" value={password} onChange={e => setPassword(e.target.value)} /></Field>{error && <p className="error">{error}</p>}<button className="button primary full">Masuk</button><p className="auth-switch">Belum punya akun? <button type="button" className="link-button" onClick={onSignup}>Daftar akun</button></p></form></main> }
export function SignupPage({ onDone }: { onDone: () => void }) { return <main className="auth-page"><form className="auth-card" onSubmit={e => { e.preventDefault(); onDone() }}><span className="brand-mark large">+</span><h1>Buat akun pasien</h1><p>Mulai akses layanan digital Anahita Hospital.</p><Field label="Nama lengkap"><input required /></Field><Field label="Email"><input required type="email" /></Field><Field label="Kata sandi"><input required type="password" /></Field><button className="button primary full">Daftar Akun</button></form></main> }
